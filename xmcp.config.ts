// MCP transport configuration for deployment platform detection
// This server uses streamable-http transport via FastMCP
export default {
  http: {
    // Streamable HTTP transport for MCP server
  },
};

